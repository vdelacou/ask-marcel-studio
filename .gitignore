# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage
lib-cov
coverage
*.lcov
.nyc_output

# Dependency directories
node_modules/
jspm_packages/

# Optional caches
.npm
.eslintcache
.stylelintcache
.node_repl_history
*.tgz
.yarn-integrity

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Build output
dist
dist-ssr
out
build
.vite
.output

# Editors / OS
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.idea

# Atelier gate scratch + output dirs (Stryker)
.stryker-tmp/
reports/

# Personal, per-developer lessons journal. The committed team journal is
# .claude/LESSONS.md — never gitignore the whole .claude/ folder.
.claude/lessons.local.md

# Electron
release/

# Vendored embedded runtimes (fetched per-machine, never committed)
vendor/

# icon build workspace: the iconset is regenerated by scripts/make-icons.sh; keep the .icns
build/icon.iconset/
